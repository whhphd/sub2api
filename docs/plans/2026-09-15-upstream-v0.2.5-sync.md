# 2026-09-15 upstream v0.2.5 同步记录

## 基线与范围

- 本地 main / origin/main、OVH 构建仓库及生产镜像起点均为 `6aa40d6f3`；两个本地仓库干净。
- 上游目标：`881f3202694c6bc932446931a30c27d9675178b9`（v0.2.5），新增 197 个历史提交（115 个非 merge 提交）。
- Git merge 融合现有二开；没有使用在线更新、reset 或覆盖长期部署目录。
- 本次仅同步、测试和提交源码；没有部署、修改生产数据库或运行配置。

## 冲突处理

1. `wire_gen.go`：采用上游提前构建 Ollama 用量服务的依赖顺序，同时保留本地代理仓库注入和余额查询的加密/配置依赖。Wire diff 验证与生成器一致。
2. `openai_ws_pool.go`：保留本地账号唯一指纹开关，同时加入上游连接关闭时从池中移除的回调；保留上游新的执行作用域、常驻读循环和连接池唤醒实现。
3. `payment_config_providers.go`：上游已完整支持 upstreamType 点号，采用上游实现及更准确的校验提示。保留 `usdt.binance` 本地回归用例。
4. `ratelimit_service.go`：代理健康依赖和 Ollama 异步探测字段并存，分别服务各自账号与错误处理路径。
5. `SettingsView.spec.ts`：保留本地指纹/随机代理池测试及上游自定义菜单按钮测试。

## 二开核对

- Grok 槽位释放 PR #6661 已被上游合入：handler 与上游一致，任务归属、等待绑定账号、取消释放、资格失败释放和计费去重测试继续存在。
- Grok OAuth 403 日志增强、可配置同账号重试、OpenAI 工具调用注入、短时 429 重试与代理切换保留。
- overdraft、5h/7d 配额耗尽暂停、自动重置卡及 `OpenAIAutoResetCreditStateExtraKey` 调度缓存保留。
- 代理健康探测与恢复、网络监控、上游余额自动查询保留。
- Channel Monitor V1 主动探测 / V2 被动聚合并行保留；Partner API 源码、配置和外部契约未改。
- 本次图片转发采用上游原生 Codex Images 及 Responses 回退；保留本地图片限额换号策略。Grok 图片/视频分组独立倍率与价格配置没有改动。

## 验证中修正

- 本地限流服务 unit 测试补齐上游新增的 Ollama 依赖参数。
- 网络采样缓存用 `ZRangeArgs(ByScore=true)` 替换被依赖标记弃用的 `ZRangeByScore`，保留毫秒时间边界及升序读取语义。
- 旧 unit 用例 `TestShouldStopOpenAIOAuth429Failover_AfterBoundedFullWindows` 错误要求第 3 次 OpenAI 换号强制终止；已在独立的 `6aa40d6f3` worktree 复现原有失败。现有本地实现由 handler 最大换号预算限制，不采用上游额外三账号截断。仅调整测试名称/断言，不修改生产重试策略；Grok 独立 follow-up 用例保留。

## 验证结果

测试代码树对应临时 Git 快照 `c939013c97ee3076c4ddb9726f4add3a9addd3f9`，最终提交仅另附本文档。

- OVH Docker Go 1.27.0：`go test ./...`、`go test -tags=unit ./...`、`go test -tags=integration ./...`、`go build -buildvcs=false ./cmd/server`。
- 集成测试启用 CI=true，通过 testcontainers 创建临时 PostgreSQL / Redis 并应用迁移；未连接生产数据服务。
- golangci-lint v2.13.0：0 issues；Wire diff 无差异。
- pnpm 9.15.9：287 个前端测试文件、2187 个用例通过；lint:check、typecheck、build（含 i18n 检查）通过。
- 仓库 CI 的 Apple 容器模拟生命周期、Compose 安全/网关环境/资源限制和 Caddy 缓存脚本通过。
- unit 服务包用时 170.975s；默认标签与集成标签的服务包也通过。容器编译显式禁用自动 VCS stamping，避免容器 root 与宿主 Git 目录属主不一致；未修改全局 Git safe.directory。
- 构建仍提示既有的 Browserslist 数据过期与大 chunk 警告，未阻止构建。

- 本轮所有验证容器使用 `--rm`，testcontainers 临时容器已退出清理；旧基线复现 worktree 已移除。测试日志保存在 OVH `/tmp/callai-v025-*.log`，本机前端日志位于 `/tmp/callai-v025-frontend-*.log`。

## 后续部署入口

- 待明确部署指令后，以最终 main 提交短哈希构建镜像；成功后 recreate，仅重建 Sub2API 服务并保留 `callai/sub2api:6aa40d6f3` 回滚镜像。
- 新增迁移：OpenCode 平台约束扩展，以及清理三档限额均为 NULL 的 user_platform_quotas 记录。集成库已验证，本次未在生产执行；部署前按规范备份数据库。
- 上游默认 compact 模型从 gpt-5.4 改为 gpt-5.5；WS 每账号连接系数从 1 改为 5（仍受 max_conns_per_account 封顶），部署时核对运行配置是否覆盖。
- 上游更新 DeepSeek Flash 官方默认价及 Pro 路由 Flash 的生效时点；分组/渠道自定义定价保持优先。
- 后续部署同步更新 Partner 媒体默认价文件的来源提交记录，并核对公开价格；不改 PriceAI 契约。
